const _ = require('lodash')
const User = require('./neo4j/user')

const retrieveProfile = function (wallet) {
    // Return the profile object of the wallet owner.

}

const updateProfile = function (wallet, changes) {
    // Update the user profile of the wallet owner using changes.
    
} 